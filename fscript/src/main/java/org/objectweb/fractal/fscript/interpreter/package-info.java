/**
 * This package contains the actual interpreter used to execute FPath and FScript
 * programs. The two main classes are {@link Interpreter} and {@link EvaluatingVisitor},
 * which respectively correspond to the classic <em>apply</em> and <em>eval</em>
 * operations. 
 */
package org.objectweb.fractal.fscript.interpreter;
