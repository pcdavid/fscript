/**
 * This is the main package of the FPath and FScript implementation. It contains the
 * definitions of the public APIs used by client programs to interact with FScript (for
 * example {@link FScriptEngine} or the {@link FScriptException} hierarchy), the {@link
 * FScript} utility class, and the implementation of some of the main internal Fractal
 * components which make the FScript engine implementation. 
 */
package org.objectweb.fractal.fscript;